import React from "react";
import { Container } from "react-grid-system";
import styled from "styled-components";

const PageContainer = styled(Container)`margin-top: 3vh;`;

export default PageContainer;
